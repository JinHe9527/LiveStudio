{$LIVESTUDIO_DOMAIN:localhost} {
    encode zstd gzip
    @objects path /livestudio/*
    handle @objects {
        reverse_proxy minio:9000
    }
    handle {
        reverse_proxy cloud:8080
    }
}
