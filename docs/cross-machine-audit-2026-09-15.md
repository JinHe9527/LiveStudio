# 2026-09-15 异机保存失败审查与 v0.1.32

## 现场线索与结论边界

使用者报告另外两台电脑均使用最新版，但无法保存；截图分别显示 `data2` 摄像头容器被拒绝，以及 Roaming/webcast_mate 下未找到通过排除检查的配置。起点为 main / 5f5d8e8，工作区干净。截图没有提供完整 LiveStudio、直播伴侣、硬件及驱动版本，只能登记为 EvidenceOnly；两台电脑目前未接入本轮执行环境。

本轮修复可从实现与隔离测试确认的问题，不宣称已经在这两台故障电脑完成恢复，不把 0.1.32 标为跨硬件已验证版。不同设备/不同画面配置的多摄像头仍无法由单一可移植配置表示，继续明确拒绝；没有靠忽略来源消除报错。

## 修复内容

- 摄像头目标和运行实例携带 data/data2 容器位置；来源枚举、设备载荷恢复、签名路径绑定、类型对比及全部实例回读使用真实容器。相同 ID 位于两个画布时保持两个实例，不以字典合并覆盖。
- 单摄像头或共享同一设备、效果配置与等价载荷的多实例可进入既有签名匹配链。对象属性顺序不再制造不同配置；数组顺序继续严格保留。
- 画布位置、来源名称等上下文字段保留在原始审计文档中，不进入可移植画面写入投影。目标画布、名称和非摄像头来源不被改写。摄像头内部的设备、滤镜名称和参数不受该上下文规则影响。
- 已有摄像头在首次停机事务中同步写入来源载荷与效果，启动后逐个回读。副画布回读失败时不调用只定位主画布的原生菜单，整次失败并回滚；不声称已支持副画布原生 UI 来源重建。
- 读取配置在同一 Windows 用户的 Roaming/webcast_mate 和 Local/webcast_mate 内有限搜索（深度最多 3，检查最多 256 个目录），不跟随目录链接，配置根确定后固定用于完整事务边界。唯一 WBStore 才选用；多份配置不按时间猜选。任意自定义安装位置、其他 Windows 用户或尚未识别存储后端没有被自动开放。
- 文件不存在、空/超大文件、权限、独占占用、JSON 无效、根节点类型和无目标字段分别给出固定诊断。存在但读坏的文件不能被丢弃后生成半份存档。设置的既有“导出兼容报告”保留该诊断，不输出解析器异常中的原始值。
- 保存与恢复使用相同版本/签名结构资格；新版未知字段不能在保存时退回旧签名适配器，避免再次生成随后无法恢复的存档。
- Windows 原子文件替换遇到错误 5/32/33 时，对同一持久临时文件最多等待 4 次、每次 100ms。持续失败仍抛错并走回滚，不删除目标、不改 ACL、不提权。

## 本机证据

环境：Windows 10.0.26200 x64、OBS 32.2.2；磁盘安装了直播伴侣 12.9.3.475022132 和 13.0.1.482583989，检查时直播伴侣进程数为 0。没有启动/停止 OBS 或直播伴侣，没有替换当前安装版 Agent，没有执行生产参数恢复。

本机 13.0.1 随包 main.js 与 27987.b81901a8.js 明确使用 data/data2 表示主/第二画布来源，并在更新跨场景同设备摄像头时访问两个容器及效果配置标识。文件 SHA-256 分别为：

- main.js：7A06A2DFF4C51DE8E751320697850442F3C036E9282CD942BADB4C154137C901。
- 27987.b81901a8.js：7F0B5A7E40FFABE6CA7B9F472E724D209A87C541B5B7349C84B5EB0E5EC36D67。

只读探针读取现有 4 份原生文档、2591 个叶字段，并以 13.0.1 版本输入完成兼容分析。当前树与内存中转换为 data2 的同一目标树均匹配既有 v4 签名矩阵；后者只证明路径绑定和结构检查，不是真实副画布运行证据，也不能证明这些落盘数据由 13.0.1 写入。

隔离文件测试覆盖 data→data2、data2→data、data2→data2、双画布同名 ID、全部实例回读、数组长度恢复、完整文档字节回滚、保留目标画布位置/名称/非摄像头、次摄像头载荷不完整时拒绝、读取失败分类、多目录歧义、短暂/持续文件占用，以及未知版本/字段回退拒绝。

证据在忽略目录 artifacts/cross-machine-audit/：environment.json、read-only-probe.json、build.log、tests.log、vulnerabilities.log、dependency-gate.log。原始配置和账号数据未加入提交或发布资产。第一次全量测试出现一次临时原子替换拒绝访问，已保留失败线索并补上有界重试与持续失败不破坏原文件的测试，不能把该次失败隐去。

## 仍需完成

两台原故障电脑须安装更新后分别在“设置 → 重新检测 / 导出兼容报告”保留精确版本和路径/类型证据，再验证保存、修改、恢复及逐字段回读。第二张截图的实际根因尚不能从本机推断；如配置位于当前搜索范围以外或不是已识别 JSON 存储，需要依据现场报告继续适配。不能声称这两台电脑已经修复完成。

正式 Verified=0。两种实体采集卡、每个版本组合连续 20 次完整循环及全部阶段回滚仍未在这两台电脑验收。

## 发布检查与结果

本地 dotnet restore、Release 整仓构建通过（0 警告、0 错误）；Core 362、Agent 57、Setup 8，共 427 项测试通过。格式检查、git diff --check、6 项依赖门测试与 15 个项目的直接/传递依赖漏洞检查通过。版本提升为 0.1.32；签名发布与公开更新发现结果将在流水线完成后补记。
