# 2026-09-08 修复与交付复验

本轮从干净的 `main / d59e5cb` 继续检查。文件清单已逐份记录路径、长度和 SHA-256（包括新增适配定义与测试共 488 份；本报告加入前），原始配置和测试存档只保留在被 Git 忽略的本机 `artifacts/delivery-audit-2026-09-08/`。

## 修复

- 修复原生扫描遗漏 JSON `null`，包括数组中的空值及其位置。
- 修复恢复较短滤镜、曲线数组时保留旧尾项；完整效果配置中的嵌套数组同样裁剪至目标长度。回读额外验证数组长度，不能再以已有前缀匹配冒充完整一致。
- 同设备出现不同配置时禁止字典覆盖。目标多摄像头歧义在 Preflight 阻断，不能写入后才发现选错对象。
- `data2` 摄像头明确阻断不完整保存和恢复；当前执行端仅支持 `data`，不再静默丢弃副容器后显示保存成功。
- 全部绑定摄像头实例必须回读一致；一个实例正确不能掩盖其他实例错误，多实例回读不一致时不再猜测原生菜单位置。
- 将 `storage/camera-payloads.json` 的存在、JSON 根类型、只读属性和目录写权限纳入事务前检查。
- 将已签名的全局画面恢复字段计入可移植覆盖清单。
- 新签名字段矩阵可以要求完整路径和类型匹配；目标缺少必需字段、出现矩阵外字段或类型改变时禁止写入。版本选择优先使用实际版本匹配的定义，避免新修订抢占旧版本。
- 增加进程级 `LIVESTUDIO_LOCAL_ONLY=1` 隔离验收模式，暂停本次 Agent 的云端和局域网发布；不改变已保存凭据。本轮真实恢复使用此模式，未主动上传测试包或原始配置。
- 打包脚本清理前验证临时目录的绝对路径和本次目录名称。

## 当前 Windows 真机

- Windows 11 Pro / 10.0.26200 / x64；OBS 32.2.2。
- 直播伴侣已由自身更新至 `12.9.3.475022132`，旧 `12.9.2` 目录已经不存在。
- 当前只有 OBS Virtual Camera，没有检测到实体采集卡。
- 相比 9 月 5 日存档，当前原生树 2591 个叶值，旧基线 2537 个；共同路径类型变化为 0。增加项包括美颜预设、塑形、空容器及本轮修复后保留的空值。只读结构报告不包含原始字段值。
- 新增精确适配 `webcast-mate-12.9.3.475022132-v4`，签名定义包含 2522 个当前目标字段，SHA-256 为 `5c48d6dbb71077d930d9aeff607d3a18fb4e89cf5c00293de45fee68604f0cca`。它要求签名字段矩阵匹配，不表示所有 12.9.3 配置都已验证。

## 验证证据

1. 原有 45 份 `.lscfg` 的嵌入签名、清单及内容哈希全部有效。
2. 真实配置的五份原生边界文件复制到隔离目录，源与副本长度/哈希一致；在副本中使用正式配置写入器和回读器检查 2522 项，写入前后差异均为 0。该项仅为隔离文件验证。
3. 首次真实恢复在写入前被驱动模式检查阻断：OBS Virtual Camera 已输出 `1080×1920@30 NV12`，直播伴侣缓存仍为 `1920×1080@30 NV12`。打开直播伴侣自己的摄像头设置后，应用更新为驱动支持的竖屏模式。未修改 OBS 输出分辨率，也未停用其虚拟摄像头。
4. 新保存 `8f3dce1d-1687-4861-a4b2-371f242d5f74` 的第一次真实联合恢复通过，耗时 24.462 秒，事务残留为 0。
5. 在直播伴侣原生美颜面板只把“美白”从 0 调到 1；正式恢复前备份保留改变后的状态。结构差异精确落在 `Foundation_ALL` 的当前值和预设值共四个关联字段，没有额外字段。随后正式联合恢复通过，2522 个目标字段逐项一致，耗时 25.737 秒，事务残留为 0。证据：`beauty-whiten-0-to-1-001.json`、`native-single-variable-restore.json`。
6. 连续 20 次同状态联合恢复全部成功，每次新增独立恢复前备份且事务残留为 0。它们是稳定性重复测试，不冒充 20 次独立保存、修改、恢复实验，也不替代跨实体采集卡验收。证据：`final-cycles.json`。

7. 七个故障点 `SessionsCreated`、`ApplicationsStopped`、`AssetsPrepared`、`SettingsApplied`、`ApplicationsStarted`、`VerificationPassed`、`ApplicationsCommitted` 均按预期失败并完整回滚。每次都有永久前置备份，五份原生文件 SHA-256 与测试前逐字节一致，事务残留为 0。故障矩阵在直播伴侣原本关闭的状态执行，避免应用重启后自身写入缓存混淆文件回滚证据。证据：`native-fault-results.json`。
8. `SettingsApplied` 和 `DurableCommitRecorded` 两处真实 Agent 进程崩溃检查通过。前者启动时恢复未提交的原始文件，后者保留已提交文件；均发现了待处理日志、处理后残留为 0，并恢复接受本机请求。证据：`crash-results.json`。

最终本机质量门：整仓 Release 构建 0 警告、0 错误；Core 300、Agent 51、Setup 8，共 359 项测试全部通过；依赖门脚本 6 项通过，15 个项目没有报告已知依赖漏洞；格式检查、PowerShell 语法和 `git diff --check` 通过。本地 0.1.26.0 MSIX 与 Setup 已使用固定内部证书签名，Setup 内嵌载荷自检成功。正式 CI、发布与安装后的验收结果另记。

## 交付边界

本机已确认的当前配置仍为 `Mapped`，`Verified=0`。不同画面配置的多摄像头、`data2` 原生恢复、未进入矩阵的美颜/插件/动态字段、其他存储后端，以及第二台不同实体采集卡电脑的规定验收均未完成。当前代码没有通过忽略字段、跳过回读或扩大未知版本权限来伪装完整支持。

固定下载入口为 [GitHub 最新正式安装器](https://github.com/JinHe9527/LiveStudio/releases/latest/download/LiveStudio-Setup.exe)，软件内更新沿用同一官方发布源。程序不主动限速，但实际下载速度取决于网络到 GitHub CDN 的线路，不能承诺固定带宽。
