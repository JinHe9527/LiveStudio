# Keep source and configuration text stable across Windows and Linux runners.
* text=auto eol=lf

# The complete OBS color-card bundle, including its manifest, is integrity-checked byte-for-byte.
# Never allow Git to rewrite line endings in any bundled asset.
src/LiveStudio.Agent/BuiltInAssets/ColorCards/v1/** -text -eol
