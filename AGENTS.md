# LiveStudio 项目协作规则

## 新对话启动

进入本仓库后，先完成以下动作，再修改代码：

1. 完整阅读 `docs/windows-validation-handoff.md`。
2. 如果任务涉及 Windows 真机、OBS、抖音直播伴侣、原生导出、适配器或恢复验收，使用仓库内 `$livestudio-windows-validation` skill。
3. 执行 `git status --short`、`git branch --show-current` 和 `git log -1 --oneline`，保留用户已有改动。
4. 阅读与当前任务直接相关的实现和测试，不能根据 README 的概述推断功能已经可靠。

## 唯一核心目标

在同一台 Windows 电脑上，完整保存、清晰展示并正确恢复 OBS 和 Windows 抖音直播伴侣的画面配置。目标字段包括：

- 视频设备选择、分辨率、FPS、像素格式、色彩空间和色彩范围。
- 全部视频滤镜的类型、启用状态、顺序、所有参数和素材引用。
- 直播伴侣全部美颜能力，包括总开关、预设、磨皮、美白、红润、肤色、清晰度、脸部与五官塑形、美妆、素材、强度、透明度。
- 直播伴侣全部曲线，包括 Master、R、G、B 通道、所有控制点、数组顺序和插值相关字段。
- 已识别目标树中的嵌套对象、数组、默认值、当前值和应用内部标识。

明确排除音频、场景布局、推流账号、Cookie、Token、推流密钥、登录态，以及 HDMI 上游相机机身参数。不得因为“全部参数”的表述把这些敏感或非目标数据加入存档。

## 当前事实

- OBS 已有基于 obs-websocket 5.x 的读取、写入和回读框架，但仍需 Windows 真机验收。
- 直播伴侣的实验扫描和原生导出 ZIP 对比只能生成探测证据，不能证明可恢复。
- 当前仓库没有来自真实 Windows 生产版本的完整签名适配定义，也没有两台不同采集卡、连续 20 次恢复一致的验收证据。
- 正式直播伴侣执行端目前只实现签名适配定义下的 `JsonFile` 存储；Registry、SQLite、LevelDB 仍需依据真机证据实现，不能伪装为已支持。
- 原生导出是否覆盖设备、视频模式、滤镜、美颜、曲线和素材尚未确认。导入成功不等于字段完整。
- 未匹配签名适配定义的直播伴侣版本只允许探测和读取，禁止写入。

任何新对话都不得把上述未完成项描述为“已完全支持”“可可靠还原”或“全部原生配置已保存”。

## 实现与验收红线

- 不维护猜测字段白名单来宣称完整；必须从真实存储结构和单变量差异建立字段清单。
- 每个目标字段都必须具备原生位置、类型、读取值、写入规则、回读规则和覆盖状态。
- Preflight 必须在任何写入前检查版本、结构指纹、设备映射、视频模式、插件、素材、磁盘空间、权限和目标场景。开播、推流和录制状态只展示，不参与恢复阻断；软件使用者负责只在未开播时操作恢复。
- 任一目标字段缺失、不可写、不可验证或回读不一致，整次恢复失败并回滚。
- 直播伴侣原生文件、数据库附属文件和 Registry 值必须按真实存储边界事务备份；不能只备份推测中的 JSON 子树。
- 探测报告只保留路径、类型与哈希。未完成敏感字段审计的原始 ZIP、配置文件和账号数据禁止提交或上传。
- “已验证”只用于两台不同采集卡电脑上完成至少 20 次保存、修改、恢复、逐字段回读和故障回滚均为 100% 的版本组合。
- UI 必须逐层展示“应用 → 视频来源 → 设备与视频模式 → 滤镜/美颜链 → 参数/曲线 → 素材”，并明确区分已保存、未覆盖、已改变、目标不支持和已验证。

## 修改后的检查

默认执行：

```shell
dotnet restore LiveStudio.slnx
dotnet build LiveStudio.slnx --configuration Release --no-restore
dotnet test LiveStudio.slnx --configuration Release --no-build
dotnet list LiveStudio.slnx package --vulnerable --include-transitive
```

涉及 Windows 真机的结论必须附带 `docs/windows-validation-handoff.md` 规定的证据，不能用 macOS 构建或单元测试代替。

## 发布纪律

- 未经用户明确要求，不创建 tag、不发布 Release、不生成对外安装包。
- 不把探测工具或实验适配器随 UI 文案包装成正式支持。
- Commit 使用中文 conventional commit：`<type>: <简短描述>`，body 使用 `-` 列表。
